# Server Configuration
PORT=8080

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=webhook_db
DB_USER=webhook_user
DB_PASSWORD=webhook_pass

# Security & Network Settings
# Set to false in production to prevent SSRF against loopback / internal private IPs
ALLOW_INTERNAL_ENDPOINTS=true
